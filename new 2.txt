rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read, write, and delete access to all documents for all authenticated users
    match /{document=**} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
